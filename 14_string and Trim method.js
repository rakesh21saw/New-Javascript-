//
// string method- action that can be performed on object.

// format
// string.Name.method()


//string.Name.method()


// TRIM Method - trim white-spaces  only from both ends not from middle of string and return a new one
//trim(): Removes whitespace from both ends of the string.

let str = '         Hello world!         ';
console.log(str.trim()); // Output: 'Hello, world!'
// 

let str1 = '         Hel    lo       ';
console.log(str1.trim()); // Output Hel    lo// sirf lasr and end wala spaces remove hote hai


// 🔹 JavaScript String Methods
// Some popular JavaScript string methods include:

// Method	             Description

// toLowerCase()	     Converts to lowercase
// toUpperCase()	     Converts to uppercase
// trim()	             Removes whitespace from both ends
// replace(old, new)	 Replaces a substring
// split(delimiter)	     Splits string into array
// indexOf(sub)	         Finds index of sub
// startsWith(sub)	     Checks if starts with sub
// endsWith(sub)	     Checks if ends with sub